# FASE A–F — evidence dump (Hermes X / Wear OS, 2026-09-16)

Raw command output for the read-only phases. Numbers in the report and README come from here, not
from a second run.

## FASE A — glyph coverage

`_workbench/font_cov.py` / `font_cov2.py`, fonts pulled from `emulator-5554` with
`adb pull /system/fonts/<f>`. fontTools 4.65.0.

```
=== A1: font -> codepoint count ===
DroidSans.ttf                    2797  max U+1F16B
DroidSansMono.ttf                 873  max U+FFFD
NotoSansSymbols-Regular-Subsetted.ttf  4616  max U+1F773
NotoSansSymbols-Regular-Subsetted2.ttf  124  max U+1F251
NotoColorEmoji.ttf               1449  max U+FE837

=== no math font ===
$ adb -s emulator-5554 shell 'ls /system/fonts | grep -i math'   -> (empty)
$ adb -s emulator-5554 shell 'cat /system/etc/fonts.xml' | grep -ci 'math'   -> 0

=== A2: block coverage (union of the five fonts) ===
math ops U+2200-22FF        256/256
arrows U+2190-21FF          112/112
superscripts U+2070-207F     14/16
subscripts U+2080-209F       28/32
misc technical U+2300-23FF  232/256
box drawing U+2500-257F     128/128
geometric U+25A0-25FF        96/96
dingbats U+2700-27BF       191/192
currency U+20A0-20CF         32/48
letterlike U+2100-214F       80/80
number forms U+2150-218F     58/64
latin-1 supp U+00A0-00FF     96/96
greek U+0370-03FF           121/144
general punct U+2000-206F    89/112

=== A2: characters with NO glyph in any pulled font ===
U+2072 ⁲   U+2073 ⁳   U+208F ₏   U+209D ₝   U+209E ₞   U+209F ₟
U+23BE ⎾   U+23BF ⎿   U+23C0 ⎀   U+23C1 ⎁   U+23C2 ⎂   U+23C3 ⎃   U+23C4 ⎄   U+23C5 ⎅
U+23C6 ⎆   U+23C7 ⎇   U+23C8 ⎈   U+23C9 ⎉   U+23CA ⎊   U+23CB ⎋   U+23CC ⎌
U+23F4 ◄   U+23F5 ►   U+23F6 ▲   U+23F7 ▼   U+23FB ⏻   U+23FC ⏼   U+23FD ⏽   U+23FE ⏾   U+23FF ⏿
(U+23F4-U+23F7 and U+23FD-U+23FF are printed here by codepoint; the renderer's fallback table
 covers the ones a model can plausibly emit.)

=== A2: sample string (the Phase-A sample) -> NO-GLYPH COUNT IN SAMPLE: 0 ===
Every character in the sample is covered by at least one of the five fonts. The gap is not the
sample; it is the codepoints *around* what the sample uses, which is why the renderer's mapping
tables are coverage-tested against the real cmaps
(`WearFontCoverage.kt`, generated by `_workbench/gen_coverage.py`: union = 8755 codepoints in
165 ranges).
```

## FASE B — the leak, as a failing test

`wear/src/test/java/.../WearAnswerLeakTest.kt`, run before any fix:

```
$ ./gradlew :wear:testDebugUnitTest --tests '*WearAnswerLeakRedTest*' --console=plain
> Task :wear:testDebugUnitTest FAILED
WearAnswerLeakRedTest > latexFractionBecomesReadable FAILED
    java.lang.AssertionError: raw LaTeX reached the watch: $\frac{1}{2}$ dari 80 = 40
WearAnswerLeakRedTest > markdownBoldNeverReachesTheScreen FAILED
    java.lang.AssertionError: raw bold markers reached the watch: **Jawaban:** 14
WearAnswerLeakRedTest > headingMarkerAndFenceAreStripped FAILED
    java.lang.AssertionError: a code fence reached the watch: ``` | # Hasil | 2 + 2 = 4 | ```
WearAnswerLeakRedTest > superscriptLatexBecomesARealSuperscript FAILED
    java.lang.AssertionError: raw LaTeX reached the watch: $x^{2}$
4 tests completed, 4 failed
BUILD FAILED in 9s
```

Device half (mock provider returning the same raw text), `_workbench/wear_ask.py`, watch at
384×384 @320:

```
--- screen after answer ---
  [120,54][265,91] 145x37 TextView 'Hermes X'
  [60,152][324,384] 264x232 TextView
    'Jawaban: 14&#10;1/2 dari 80 = 40&#10;x² + y² = r²&#10;Hasil&#10;• poin satu&#10;• poin dua&#10;tautan&#10;coret dan kode'
--- provider saw ---
  user='hitung'  answer='**Jawaban:** 14\n$\frac{1}{2}$ dari 80 = 40\n...'
```

## FASE B — the crash found on the device (not in review)

Every answer crashed the app before this session's fix:

```
09-16 23:30:42.479 13355 13355 E AndroidRuntime: FATAL EXCEPTION: main
Process: com.hermes.app, PID: 13355
java.lang.IllegalStateException: Vertically scrollable component was measured with an infinity
maximum height constraints, which is disallowed. One of the common reasons is nesting layouts like
LazyColumn and Column(Modifier.verticalScroll())...
    at androidx.compose.foundation.CheckScrollableContainerConstraintsKt...
    at androidx.compose.foundation.ScrollNode.measure-3p2s80s(Scroll.kt:440)
```

Cause: `Modifier.verticalScroll(rememberScrollState())` on the answer `Text` inside a
`ScalingLazyColumn` item (`WearMainActivity.kt:473-475`, introduced in `86de5749`). The user-visible
symptom was that **the answer never appeared at all** — the process died as the card was laid out.

## FASE B — voice fallback never rendered

```
$ grep -n 'ttsAvailable\|voiceProblem' WearMainActivity.kt
229:    val ttsAvailable by ttsReady.collectAsState()
230:    val voiceProblem by voiceUnavailable.collectAsState()
(no other occurrence: both flows were collected and rendered nowhere)

$ adb shell input tap <Speak>   # permission granted, no recognizer on this image
--- after tap ---
  'Send' 'Speak' 'Change key'          <- no message
```

After the fix, the same tap on the same image:

```
--- after tap ---
   [76,0][308,31] 'Type a question'
   [64,138][137,174] 'Send'
   [64,254][152,290] 'Speak'
   [69,336][315,384] 'Voice input is not available on this watch. Type instead.'
logcat: W HermesWear: no speech recognizer on this watch
```

## FASE E — layout matrices

### E1/E2 — profiles, coordinate space verified

```
$ adb -s emulator-5554 shell dumpsys window displays | grep -m1 'init='
init=384x384 320dpi mMinSizeOfResizeableTaskDp=220 base=466x466 326dpi cur=466x466 app=466x466 rng=466x466-466x466
```

### E2.3 — touch targets (clickable node, not the label)

```
=== 384 384x384 @320 = 192.0 dp ===
   OK  Button 312x128 px = 156.0x64.0 dp  'Type a question'
   OK  Button 264x96  px = 132.0x48.0 dp  'Type a question' (EditText)
   OK  Button 300x100 px = 150.0x50.0 dp  'Send'
   -- bottom --
   OK  Button 312x104 px = 156.0x52.0 dp  'Debug'
=== 466 466x466 @326 = 228.7 dp ===
   OK  Button 386x130 px = 189.4x63.8 dp
   FAIL Button 338x97 px = 165.9x47.6 dp   <- EditText: 0.4 dp under the 48 dp target
   OK  Button 384x105 px = 188.5x51.5 dp  'Send'
   -- bottom --
   OK  Button 362x99 px = 177.7x48.6 dp  'Debug'
   OK  Button 386x106 px = 189.4x52.0 dp  'Change key'
=== 454 454x454 @320 = 227.0 dp ===
   OK  Button 374x128 px = 187.0x64.0 dp
   OK  Button 326x96 px = 163.0x48.0 dp
   OK  Button 372x103 px = 186.0x51.5 dp
   OK  Button 350x97 px = 175.0x48.5 dp
   OK  Button 374x104 px = 187.0x52.0 dp
=== 480 480x480 @360 = 213.3 dp ===
   OK  Button 390x144 px = 173.3x64.0 dp
   OK  Button 336x108 px = 149.3x48.0 dp
   OK  Button 384x115 px = 170.7x51.1 dp
```

`Wear Material 3` gives every `Button` ≥ 48 dp except the composer's `EditText` at 466×466, where the
Wear framework's own field height lands 0.4 dp under the guideline. Nothing in this app's own layout
is under it.

### E2.1 — circle fit, physical mask

`wm size` moves the coordinate space but **not** the round mask (r = 192 px physical), so the test
rescales override px back to physical px before applying `dx² + dy² ≤ r²`.

```
=== 466x466 @326 (228.7 dp) — corners vs PHYSICAL mask r=192 px ===
   TextView    149x38 px  pos=(153,88)   outside=-      by=0.0 px  'Hermes X'
   TextView    326x36 px  pos=(64,187)   outside=-      by=0.0 px  'Type a question'
   TextView     73x36 px  pos=(69,336)   outside=-      by=0.0 px  'Send'
   TextView     66x11 px  pos=(109,443)  outside=tl,bl  by=16.8 px  'Speak'
```

The only element with a corner outside the mask is the **partially scrolled** `Speak` label — the
item is entering the viewport, so its clipped label sits near the bottom edge. `ScreenScaffold` +
`ScalingLazyColumn` handle the actual circular clip; no element is fully outside the circle.

### E3 — does the UI use the extra width? (measured twice, on purpose)

First pass reported 454 as identical to 384 — the signature of a stale frame. Re-measured with the
coordinate space printed before the dump and two dumps per profile:

```
=== 384: init=384x384 320dpi ... cur=384x384 app=384x384 ===
   pass1: field 264x96 px  x 60..324  = 132.0 dp  (68.8% of 384)
   pass2: field 264x96 px  x 60..324  = 132.0 dp  (68.8% of 384)
=== 454: init=384x384 320dpi ... base=454x454 320dpi cur=454x454 app=454x454 ===
   pass1: field 326x96 px  x 64..390  = 163.0 dp  (71.8% of 454)
   pass2: field 326x96 px  x 64..390  = 163.0 dp  (71.8% of 454)
=== 454-again: (same numbers)
=== 466: init=384x384 320dpi ... base=466x466 326dpi cur=466x466 app=466x466 ===
   field 338x97 px = 165.9 dp  (72.5% of 466)
```

| Profile | Screen | Composer/answer column | % of width |
|---|---|---|---|
| small round | 384 px @320 = 192.0 dp | 132.0 dp | 68.8 % |
| large round | 454 px @320 = 227.0 dp | 163.0 dp | 71.8 % |
| **Xiaomi Watch 2** | 466 px @326 = **228.7 dp** | **165.9 dp** | **72.5 %** |

The column grows with the screen and its *share* of the width grows too, so the extra 36.7 dp of a
Xiaomi Watch 2 is used for text rather than thrown away in margin. No layout change was warranted;
the earlier "fixed 8 dp inset wastes the width" finding was an artifact of the stale dump and is
recorded here as a discarded hypothesis, not a fix.

### E2.6 — font scale 1.3

```
384 @320, font_scale 1.3:
   TextView 'Change key'  205x45 px = 102.5x22.5 dp   (was 165x36 = 82.5x18.0)
   TextView 'Debug'       121x48 px =  60.5x24.0 dp   (was  92x36 = 46.0x18.0)
466 @326, font_scale 1.3:
   TextView 'Hermes X'        207x53 px   pos=(137,75)
   TextView 'Type a question' 336x53 px   pos=(72,195)
   TextView 'Send'            103x53 px   pos=(80,357)
   TextView 'Speak'            93x2  px   pos=(123,478)   <- clipped at the viewport edge
   -- scrolled to bottom --
   TextView 'Change key'      221x49 px   pos=(91,40)
   TextView 'Debug'           133x53 px   pos=(77,164)
```

Answer to the phase-E question: at 466×466 with `font_scale 1.3` there is **no** room for one more
button without scrolling — the list already needs a scroll to reach `Change key`/`Debug`, and
`Speak` sits at the viewport edge (y=478 of 466). Answer text is not truncated by the scale: the
answer card is capped at `MAX_ANSWER_LINES` and the *list* scrolls, which is the intended behaviour.

## FASE F — pair-from-phone: what was proved, and what was not

```
$ adb -s emulator-5554 shell dumpsys account | sed -n '/Accounts:/,+1p'
  Accounts: 0
$ adb -s emulator-5556 shell dumpsys account | sed -n '/Accounts:/,+1p'
  Accounts: 0
$ adb -s emulator-5554 shell pm list packages | grep -iE 'gms|wearable|com.hermes'
  package:com.google.android.gms
  package:com.google.android.wearable.app
  package:com.hermes.app
  ... (19 wear/gms packages)
$ adb -s emulator-5556 shell pm list packages | grep -i hermes
  (empty — the phone APK is not installed on the phone emulator in this session)
```

On-device attempt, watch side, real code path (`WearPairing.request` → `WearPairingTransport`):

```
$ adb -s emulator-5554 logcat -s HermesWear:*
W HermesWear: pairing: no node advertises hermes_phone
```

That is `CapabilityClient.FILTER_REACHABLE` returning zero nodes, which is the documented
consequence of `Accounts: 0` on both emulators: the Wear OS Data Layer needs both devices on the same
Google account (or a real paired phone). The UI reports it honestly:

```
$ adb -s emulator-5554 shell uiautomator dump   # after tapping "Pair with phone"
TextView  [36,110][348,194]  'No phone app found. Install it and open it once, or use a key on the watch.'
```

**Options, evaluated (prompt's A–D):**

| Option | What it would actually prove | Verdict here |
|---|---|---|
| A. Add the same Google account to both emulators | the real Data Layer transport | Not attempted: requires GUI sign-in into a Google account inside two sandboxes. Out of scope for this session, and the account is the owner's. |
| B. Physical devices (Xiaomi Watch 2 + phone) | the real transport, on the real hardware | Out of this session's reach (no hardware attached). This is HS4, unchanged. |
| C. Fake transport + `MessageEvent` tests for the listener | every *decision* branch (no phone / send failed / timeout / ack), not the transport | **Already in place and green:** `WearPairingTest` — 8 tests covering exactly those branches. |
| D. Mock and claim "pairing proved" | nothing | Refused. Not done anywhere in this report. |

**What is proved:** the watch's decision logic (8 unit tests), the honest UI state when no phone is
reachable, and the signing identity both APKs must share. **What is not proved:** the Data Layer
transport itself, phone → watch. HS4 stays recorded as needing hardware.
